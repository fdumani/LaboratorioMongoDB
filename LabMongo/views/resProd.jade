extends layout
block content

    form#Home(name="", method="get",action="/")
        button#submit_home(type="submit") Home
    br
    br

    h1.
        Pelicula buscada:
    form#form_add_student(name="buscarProd", method="get",
    action="/buscarProd")
        ul
            each pelicula, i in peli
                li#pelicula_list_item Nombre: #{pelicula.nombre}; Genero: #{pelicula.genero}; Año: #{pelicula.year}
                br
            br
            br
        h3.
            Promedio de duración de películas: #{durc} minutos
        
        button#submit_student(type="submit") Buscar otra pelicula